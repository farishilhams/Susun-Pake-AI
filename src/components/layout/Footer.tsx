"use client";

// ============================================================
// components/layout/Footer.tsx — Global Footer Susun Pake AI
// Re-exports CinematicFooter (GSAP ScrollTrigger)
// Bebas tombol "Kembali ke Atas", salinan ringkas 1 baris,
// dan konten terintegrasi 100% untuk produk Susun Pake AI.
// ============================================================

import React from "react";
import { CinematicFooter } from "@/components/ui/motion-footer";

export default function Footer() {
  return <CinematicFooter />;
}
